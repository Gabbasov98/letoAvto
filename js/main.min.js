$("._tab").click(function() {
    let parentBlock = $(this).parents("._tabs-parent")
    let tabId = $(this).attr("data-tab")
    $(parentBlock).find("._tab").removeClass("_active")
    $(this).addClass("_active")
    $(parentBlock).find(".tab-content").removeClass("_active")
    $(parentBlock).find(`.${tabId}`).addClass("_active")
})

$(".qa-card__show").click(function() {
    $(this).parents(".qa-card").toggleClass("_active")
    $(this).siblings(".qa-card__hidden").slideToggle()
})

$(".favourite-btn").click(function() {
    $(this).toggleClass("_added")
})

$("._js-show-all-brands").click(function() {
    $(this).parents(".brands").toggleClass("_show-all")
})

$(document).on('click', '.rating-input__item', function(e) {
    let ratingItem = parseInt($(this).attr("data-rating-value"))
    $(this).parent(".rating-input").attr("data-total-rating", ratingItem)
    $(this).siblings(".rating-input__field").val(ratingItem)
})

$("._toggle-menu").click(function () {
    $(".header").toggleClass("_open")
    $("body").toggleClass("fixed-body")
})

$(".nav-item__top").click(function() {
    $(this).parents(".nav-item").toggleClass("_active")
    $(this).siblings(".nav-item__bottom").slideToggle()
})

$(".header-catalog__item").mouseover(function () {
    if(window.innerWidth<=992){
        return
    }

    let id = $(this).attr("data-id")

    $(".header-catalog__item").removeClass("_active")
    $(this).addClass("_active")

    $(".submenu").removeClass("_active")
    $(`.submenu[data-id="${id}"]`).addClass("_active")
})

$(".header-catalog__item").click(function () {
    let id = $(this).attr("data-id")

    $(".header-catalog__item").removeClass("_active")
    $(this).addClass("_active")

    $(".header").addClass("_open-sub-catalog")
    $(".submenu").removeClass("_active")
    $(`.submenu[data-id="${id}"]`).addClass("_active")
})

$(".header-top__back").click(function () {
    if($(".header").hasClass("_open-sub-catalog")){
        $(".header").removeClass("_open-sub-catalog")
    } else{
        $(".header").removeClass("_open-catalog")
    }
})

$("._toggle-catalog").click(function () {
    $(".header").toggleClass("_open-catalog")
    $("body").toggleClass("fixed-body")

    if(window.innerWidth<=992){
        $(".header").addClass("_open")
    }
})

$("._toggle-catalog2").click(function () {
    $(".header").toggleClass("_open-catalog")
})

function Sliders() {
    let SlidersArray = []

    let sliders = document.querySelectorAll(".slider")
    sliders.forEach((slider,index) => {
        slider.setAttribute("data-slider-id",index)
        let isLoop = false
        if(slider.classList.contains("_loop")){
            isLoop = true
        }

        SlidersArray[index] = new Swiper(`.slider[data-slider-id="${index}"] .swiper-container`, {
            slidesPerView: "auto",
            spaceBetween: 0,
            watchSlidesProgress: true,
            preventClicks :true,
            a11y: false,
            observer: true,
            observeParents: true,
            observeSlideChildren: true,
            loop: isLoop,
            navigation: {
                nextEl: `.slider[data-slider-id="${index}"] .swiper-button-next`,
                prevEl: `.slider[data-slider-id="${index}"] .swiper-button-prev`,
            },
            pagination: {
                el: `.slider[data-slider-id="${index}"] .swiper-pagination`,
                type: 'bullets',
                clickable: true,
            },
        })
    })
}

function gallery() {
    let SlidersArray = []

    let sliders = document.querySelectorAll(".gallery")
    sliders.forEach((slider,index) => {
        slider.setAttribute("data-slider-id",index)
        let gallery = {
            mySwiper: {},
            mySwiper2: {}
        }

        gallery.mySwiper = new Swiper(`.gallery[data-slider-id="${index}"] .mySwiper`, {
            spaceBetween: 0,
            slidesPerView: "auto",
            freeMode: true,
            watchSlidesProgress: true,
        });
        gallery.mySwiper2 = new Swiper(`.gallery[data-slider-id="${index}"] .mySwiper2`, {
            spaceBetween: 0,
            loop: true,
            navigation: {
                nextEl: `.gallery[data-slider-id="${index}"] .swiper-button-next`,
                prevEl: `.gallery[data-slider-id="${index}"] .swiper-button-prev`,
            },
            thumbs: {
                swiper:  gallery.mySwiper,
            },
        });

        SlidersArray[index] = gallery
    })

}


Sliders()
gallery()

$(".color-radio input[name='color']").change(function () {
    let value = $(this).attr("data-value")
    let connectedInput = $(`.color-radio input[name='color2'][data-value="${value}"]`)

    $(connectedInput).prop("checked",true);
    $(connectedInput).trigger("change");
})

$(".color-radio input[name='color2']").change(function () {
    let value = $(this).attr("data-value")
    let connectedInput = $(`.color-radio input[name='color'][data-value="${value}"]`)

    if(connectedInput.length !== 0){
        $(connectedInput).prop("checked",true);
    }else{
        $(".color-radio input[name='color']").each(function () {
            $(this).prop("checked",false);
        })
    }

    $("._js-color-output").html(value)
})

$(".color-radio input[name='material']").change(function () {
    let value = $(this).attr("data-value")
    $("._js-material-output").html(value)
})

$(window).on('load resize', function(){
    if ($(window).width() <= 992) {
        $('.product-cart').insertAfter('.product-top__title');
        $('.header-contacts').insertBefore('.header-search');
        $('.header-catalog').insertAfter('.header-search');
    } else{
        $('.product-cart').insertBefore('.about-marketplaces');
        $('.header-contacts').insertAfter('.header-bottom__col');
        $('.header-catalog').insertAfter('.header-bottom__col');
    }
});

$(".filter-group__top").click(function () {
    $(this).parents(".filter-group").toggleClass("_hide")
    $(this).siblings(".filter-group__bottom").slideToggle()
})

$(`[name="delivery-way"]`).change(function () {
    let value = $(this).attr("data-value")
    $(this)
        .parents(".order-form__group")
        .find(".order-form__variant")
        .removeClass("_active")

    $(`[data-val="${value}"]`).addClass("_active")
})

let fileInput = document.querySelector("[data-upload-id='file-input']")

if(fileInput){
    console.log(fileInput)
    const upload = new FileUploadWithPreview.FileUploadWithPreview('file-input');

    upload.options.multiple = true

    $(document).on('change', '.input-container input', function(e) {
        $(this).parents(".file-input").addClass("_uploaded")
    })

    $(document).on('click', '.image-preview-item-clear', function(e) {
        let count = $(this).parents(".file-input").find(".image-preview-item").length
        if(count === 1){
            $(this).parents(".file-input").removeClass("_uploaded")
        }
    })
}



let body = document.querySelector("body")
let modalTogglers = document.querySelectorAll("[data-modal]")
let modals = document.querySelectorAll(".modal")

modalTogglers.forEach(el => {
    el.onclick = function () {
        let modalId = el.getAttribute("data-modal")
        openModal(modalId)
    }
})

function closeModals() {
    modals.forEach(modal => {
        modal.classList.remove("_active")
        body.classList.remove("fixed-body2")
    })
}

function openModal(modalId) {
    closeModals()
    let modal = document.querySelector(`#${modalId}`)
    let closeButtons = modal.querySelectorAll(`._close-modal`)

    modal.classList.add("_active")
    body.classList.add("fixed-body2")


    closeButtons.forEach(btn => {
        btn.onclick = ()=> {
            modal.classList.remove("_active")
            body.classList.remove("fixed-body2")
        }
    })
}

var getScrollBarSize = (function() {
    var el = window.document.createElement('textarea'), style = {
        'visibility': 'hidden', 'position': 'absolute', 'zIndex': '-2147483647',
        'top': '-1000px', 'left': '-1000px', 'width': '1000px', 'height': '1000px',
        'overflow': 'scroll', 'margin': '0', 'border': '0', 'padding': '0'
    }, support = el.clientWidth !== undefined && el.offsetWidth !== undefined;

    for (var key in style) {
        if (style.hasOwnProperty(key)) {
            el.style[key] = style[key];
        }
    }

    return function() {
        var size = null;
        if (support && window.document.body) {
            window.document.body.appendChild(el);
            size = [el.offsetWidth - el.clientWidth, el.offsetHeight - el.clientHeight];
            window.document.body.removeChild(el);
        }

        document.documentElement.style.setProperty('--scrollWidth', `${size[0]}px`);
        return size;
    };
})();


window.addEventListener('DOMContentLoaded', () => {
    fix100vh();
    window.addEventListener('resize', () => {
        fix100vh();
        getScrollBarSize()
    })

    getScrollBarSize()

    $('input[type="tel"]').mask('+7 (999) 999-99-99', { autoclear: false }, { placeholder: '+7 (   )    -  -  ' });
    $(".fg select").niceSelect()
})

function fix100vh() {
    let vh = window.innerHeight * 0.01;
    document.documentElement.style.setProperty('--vh', `${vh}px`);
}

//# sourceMappingURL=../sourcemaps/main.min.js.map
